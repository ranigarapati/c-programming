#include<stdio.h>
void main()
{
    int x,temp;
    printf("enter a number");
    scanf("%d",&x);
    temp=checkevenodd(x);
    if(temp==0)
    printf("EVEN");
    else
    printf("ODD");
}
int checkevenodd(int x)
{
    if(x%2==0)
    return (0);
    else
    return (1);
}
enter a number
6
EVEN
