#include<stdio.h>
int main()
{
    char str[50];
    int i=0;
    printf("enter a string");
    gets(str);
    while(str[i]!='\0')
    {
        if(str[i]=='a')
        str[i]=str[i]-32;
        i++;
    }
    printf("adter converting a into uppercase the string becomes:");
    puts(str);
    }
 output:
 enter a string:aditya
 After converting a into uppercase the string becomes:
 AdityA
